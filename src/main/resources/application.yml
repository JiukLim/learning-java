spring:
  datasource:
    url: jdbc:h2:file:/Users/user/h2-demo
    driver-class-name: "org.h2.Driver"
    username: sa
    password: password
  jpa:
    database-platform: "org.hibernate.dialect.H2Dialect"
  h2:
    console:
      enabled: true # http://localhost:8080/h2-console 로 접속 가능.
